external static resources for if.then.fund
==========================================

To update this repository, get the prereqs:

	npm install -g webfont-dl

Then:

	./build.sh
	git commit -am "update"
